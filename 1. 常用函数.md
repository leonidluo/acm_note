## 常用函数与作用
* `sort(a,a+n)` //a数组从0到n-1排序
* `to_string(k)` //返回一个**string**内容为数字**k**
* [字符函数](#字符函数)  
* `reverse(s.begin(),s.end())` //翻转字符串  
* `next_permutation(s.begin(), s.end())` //下一个全排列  
* `reverse(a.begin(),a.end();`//翻转向量
* `upper_bound(a+i,a+j,x)`//a返回的是第一个大于x的数的坐标  
* `lower_bound(a+i,a+j,x)`//a返回的是第一个小于x的数的坐标

