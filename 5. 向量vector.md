## 5.vector  


向量的**定义**:  
`vector<int> ai; `   
`vector<int> abc; `   
//初 始 化 一 个 size 为 0 的 vector， 最 常用   
// 初 始 化 size, 但 每 个 元 素 值 为 默认 值 

`vector <int> abc(10) ; `  
// 初 始 化 了 10 个 默认 值 为 0 的 元 素 
// 初 始 化 size, 并 且 设 置 初 始 值  

`vectork <int> cde(10，1);`   
// 初 始 化 了 10 个 值 为 1 的 元 素  
`push_back(value);`  
// 往 尾 部 添加 一 个 元 素 : 向 量 名.  
`pop_back();`   
//删除尾部的第一个元素
